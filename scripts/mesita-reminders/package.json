{
  "name": "mesita-reminders",
  "private": true,
  "type": "module",
  "description": "Scripts standalone (fuera del build de Vite) que corren en GitHub Actions una vez por día y avisan (email y, cuando esté disponible, WhatsApp) a quienes tienen Mesita al día siguiente.",
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "nodemailer": "^6.9.15"
  }
}
